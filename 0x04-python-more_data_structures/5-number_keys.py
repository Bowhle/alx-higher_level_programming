#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    Returns the number of keys in a dictionary.

    Args:
        a_dictionary: The dictionary whose keys are to be counted.

    Returns:
        The number of keys in the dictionary.
    """
    return len(a_dictionary)
